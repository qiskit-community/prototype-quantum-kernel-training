[tox]
minversion = 3.8
envlist = py37, py38, py39, py310
skipsdist = True

[testenv]
deps =
    treon
usedevelop = true
setenv =
  VIRTUAL_ENV={envdir}
  LANGUAGE=en_US
  LC_ALL=en_US.utf-8
commands =
  pip check
  python -m treon docs/ --threads 5
